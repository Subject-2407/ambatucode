// Placeholder shell. The UI is owned by the frontend jobdesk; this file exists
// so the App Router has a root route while the API surface is built out.
export default function HomePage() {
  return (
    <main>
      <h1>Ambatucode</h1>
      <p>API server is running.</p>
    </main>
  );
}
