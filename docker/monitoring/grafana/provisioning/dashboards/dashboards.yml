apiVersion: 1

providers:
  - name: ambatucode
    folder: Ambatucode
    type: file
    # Provisioned dashboards are the source of truth; an edit made in the UI
    # is lost on restart unless it is exported back into this directory.
    allowUiUpdates: false
    options:
      path: /etc/grafana/dashboards
